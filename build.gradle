apply plugin: 'java'

repositories {
  jcenter()
}

dependencies {
  testCompile 'junit:junit:4.12'
}

test {
  testLogging {
    exceptionFormat 'full'
    // events 'started', 'skipped', 'passed', 'failed'
  }
}
